model   MeshGraphNets
mode    inference  # Train / Inference
gpu_ids 3    # -1 for CPU, GPU ids for multi-GPU training (DDP with both H200s)
log_file_dir    ex1/infer2.log
modelpath   ./outputs/ex1/model4.pth
%   Datasets
dataset_dir ./dataset/ex1.h5
infer_dataset   ./dataset/hex_dataset.h5
infer_timesteps 1
%   Common params
input_var   4   # number of input variables: x_disp, y_disp, z_disp, stress (excluding node types)
output_var  4   # number of output variables: x_disp, y_disp, z_disp, stress (excluding node types)
feature_loss_weights  1, 1, 0.01, 1.0  # Per-feature loss weights for [x_disp, y_disp, z_disp]
edge_var    8   # deformed dx/dy/dz/dist + reference dx/dy/dz/dist
positional_features  4   # Rotation-invariant node features: [centroid_dist, mean_edge_len, + encoding]
'
%   Network parameters
message_passing_num 15
Latent_dim	128	# MeshGraphNets latent dimension
'
% Memory Optimization
use_checkpointing   True
'
% Performance Optimization
use_amp             True    # Mixed precision training with bfloat16 (1.5-2x speedup on Ampere+ GPUs)
use_ema             True    # EMA shadow model for validation/inference (smooths SGD noise, ~free precision)
ema_decay           0.99   # EMA decay factor (0.999 = ~1000-step averaging window)
'
% Node Type Parameters
use_node_types  True    # Add one-hot encoded node types to node features
'
% World Edge Parameters
use_world_edges         False
'
% Multi-Scale / Hierarchical Parameters (BFS Bi-Stride, ICML 2023)
use_multiscale      False
